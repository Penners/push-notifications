"use client";
import { useState } from "react";

export const NotificationBuiler: React.FC<{}> = () => {
  const [state, setState] = useState({});

  return (
    <div>
      <form></form>
    </div>
  );
};
